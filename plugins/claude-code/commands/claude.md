---
description: Zibbo — Claude Code activation status (gateway view)
allowed-tools: Bash(zibbo), Bash(zibbo *)
---

!`zibbo claude $ARGUMENTS`

Present the output above verbatim, in a fenced code block. It shows the gateway's view of
routing and observed authentication. If the `zibbo` command was not found, tell the user to
install it: `pipx install git+https://github.com/MohammedZaid-AI/zibbo`.
