---
description: Zibbo — replay content through the pipeline (no upstream call)
argument-hint: "[--content <text> | --file <path>]"
allowed-tools: Bash(zibbo), Bash(zibbo *)
---

!`zibbo benchmark $ARGUMENTS`

Present the output above verbatim, in a fenced code block. Do not summarize or invent
numbers. With no argument it replays the last request's metadata. If the `zibbo` command was
not found, tell the user to install it: `pipx install git+https://github.com/MohammedZaid-AI/zibbo`.
