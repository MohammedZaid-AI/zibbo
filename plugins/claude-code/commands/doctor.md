---
description: Zibbo — diagnose gateway, routing, authentication, cache
allowed-tools: Bash(zibbo), Bash(zibbo *)
---

!`zibbo doctor $ARGUMENTS`

Present the output above verbatim, in a fenced code block. Do not summarize or invent
numbers. Each failing check lists its own fix. If the `zibbo` command was not found, tell
the user to install it: `pipx install git+https://github.com/MohammedZaid-AI/zibbo`.
