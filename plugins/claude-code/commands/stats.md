---
description: Zibbo — token & cost savings, today and all-time
allowed-tools: Bash(zibbo), Bash(zibbo *)
---

!`zibbo stats $ARGUMENTS`

Present the output above verbatim, in a fenced code block. Do not summarize or invent
numbers. If the gateway is not reachable, run `/zibbo:start`. If the `zibbo` command was not
found, tell the user to install it: `pipx install git+https://github.com/MohammedZaid-AI/zibbo`.
