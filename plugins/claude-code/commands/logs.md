---
description: Zibbo — recent optimization activity (metadata only)
allowed-tools: Bash(zibbo), Bash(zibbo *)
---

!`zibbo logs $ARGUMENTS`

Present the output above verbatim, in a fenced code block. This is metadata only — never
prompt contents. If the `zibbo` command was not found, tell the user to install it:
`pipx install git+https://github.com/MohammedZaid-AI/zibbo`.
