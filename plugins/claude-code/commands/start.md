---
description: Zibbo — start the local gateway if it is not already running
allowed-tools: Bash(zibbo), Bash(zibbo *)
---

!`zibbo start $ARGUMENTS`

Present the output above verbatim. If it reports the gateway is not installed, tell the user
to install it: `pipx install git+https://github.com/MohammedZaid-AI/zibbo`.
