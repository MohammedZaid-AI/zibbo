---
description: Zibbo — stop routing Claude Code through Zibbo (the gateway keeps running)
allowed-tools: Bash(zibbo), Bash(zibbo *)
---

!`zibbo disconnect $ARGUMENTS`

Present the output above verbatim. If the `zibbo` command was not found, tell the user to
install it: `pipx install git+https://github.com/MohammedZaid-AI/zibbo`.
